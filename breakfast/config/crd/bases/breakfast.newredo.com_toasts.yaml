---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: toasts.breakfast.newredo.com
spec:
  group: breakfast.newredo.com
  names:
    kind: Toast
    listKind: ToastList
    plural: toasts
    singular: toast
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Toast is the Schema for the toasts API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ToastSpec defines the desired state of Toast
            properties:
              toastiness:
                description: Toastiness is how long to cook the toast, from 0 (raw)
                  to 10 (blackend)
                type: integer
            type: object
          status:
            description: ToastStatus defines the observed state of Toast
            properties:
              lastUpdated:
                description: LastUpdated is the last time the Toast was updated
                format: date-time
                type: string
              ready:
                description: Ready is whether the Toast has been fully cooked
                type: boolean
              toastiness:
                description: Toastiness is the current level of toastiness
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
